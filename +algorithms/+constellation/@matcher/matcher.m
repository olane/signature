classdef matcher
    %MATCHER Matches audio clips using the pre-built database
    %   Using the audio fingerprint database built by the builder, this
    %   tries to find close matches to a given audio clip.
    
    properties
    end
    
    methods
    end
    
end

