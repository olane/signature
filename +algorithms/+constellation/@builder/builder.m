classdef builder
    %BUILDER Builds the fingerprint database for this algorithm
    %   Takes audio data, creates an audio fingerprint from it, and inserts
    %   it into a database. These fingerprints can be used later by the
    %   matcher to match audio clips.
    
    properties
    end
    
    methods
    end
    
end

